<%= form_with scope: :task, url: tasks_path, html: {class: "task-form"} do |f| %>
    <h2>
        <%= f.label :task_name, "Task Name" %>: <br>
        <%= f.text_area :title %> 

        <%= f.submit %>
    </h2>
<% end %>
