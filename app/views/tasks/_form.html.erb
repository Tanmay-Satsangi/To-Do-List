<%= form_with scope: :task, url: tasks_path, html: {class: "task-form"} do |f| %>
    <h2>
        <%= f.label :title, label_text %>: 
        <%= f.text_area :title %> 

        <%= f.submit "#{button_name}" %>
    </h2>
<% end %>
