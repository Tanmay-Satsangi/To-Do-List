<%= form_with model: @task do |f| %>
    <h2>
        <%= f.label :title %>: 
        <%= f.text_area :title %> 

        <br>

        <%= f.label :deadline %>
        <%= f.text_area :deadline %>
        <br>

        <%= f.submit %>
    </h2>
<% end %>
