<%= form_with model: @task do |f| %>
    <h2>
        <%= f.label :title %>: 
        <%= f.text_area :title %> 

        <br>

        <%= f.label :deadline %>
        <%= f.datetime_field :deadline %>
        <br>

        <%= f.submit %>
    </h2>
<% end %>
